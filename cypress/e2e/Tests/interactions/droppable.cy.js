const { it } = require('mocha');

describe('Cypress Challenge', () => {
	beforeEach(() => {
		cy.visit('/droppable');
	});

	it('This is your test case one title', () => {});
	it('This is your test case one title', () => {});
});
